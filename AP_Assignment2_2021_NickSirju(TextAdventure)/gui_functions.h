#pragma once

#include "STLINCLUDE.h"

namespace gui
{
	const string game_menu_title(const string title);
	const string game_menu_divider(const unsigned amount = 30, const char symbol = '-');
	const string game_menu_item(const unsigned index, const string text);


}